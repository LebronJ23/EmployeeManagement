@page "/pages/details/{id}"
@model DetailsModel

<div class="m-2">
    <partial name="_EmployeeEditForm" model="@Model.ViewModel" />
</div>


@functions {
    public class DetailsModel : BaseManagementPageModel
    {
        public DetailsModel(DataContext context) : base(context)
        {

        }

        public async Task OnGetAsync(long id)
        {
            Employee employee = await Context.Employees.Include(e => e.Department).FirstOrDefaultAsync(e => e.Id == id);
            ViewModel = EmployeeViewModelFactory.Details(employee);
        }
    }
}